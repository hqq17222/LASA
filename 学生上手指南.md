# 学生上手指南

> 写给接手「拉萨南北山绿化监测平台 + 外业调查 App」维护工作的同学。
> 读完这份指南，你应该能：本地跑起来 → 改代码 → 部署到服务器 → 打包 App。

---

## 0. 你接手的到底是什么

- **监测平台**：http://106.15.35.204:18480/ —— 部署在阿里云服务器（Linux），架构 = nginx（对外 18480 端口）→ supervisor 托管的 FastAPI 后端（内网 18481 端口）→ SQLite 数据库。前端是 Vue 3 单页应用，构建产物由 nginx 直接服务。
- **安卓外业 App**：给野外样地调查的研究生用，功能是拍照上传（自动解析 GPS 定位）、记录轨迹、物种/郁闭度等注释。它是个 WebView 混合壳 + 原生后台轨迹服务，账号体系和平台共用。
- **你的角色**：后续的功能改进与日常维护。代码都在这个仓库里。

## 1. 第一天：找黄老师拿三样东西

| 物品 | 用途 | 注意 |
|---|---|---|
| GitHub 仓库 Collaborator 权限 | 拉取/推送代码 | 提供你的 GitHub 用户名给老师 |
| 服务器 root SSH 密码 | 部署 | 保存为 `scripts/.lasa_pass`（一行密码，**已在 .gitignore 里，绝不提交**） |
| 平台测试账号密码 | 登录验证 | 四级账号：admin / manager / analyst / viewer |

服务器上的部署目录是 `/opt/lasa-nanshan-platform`。

## 2. 仓库结构速览

```
LASA/
├── backend/          FastAPI 后端（app/routers/ 下每个文件对应一类 API）
├── frontend/         Vue3 + Vite + Leaflet（src/views/ 下每个文件对应一个页面）
├── android-app/      安卓 App（Kotlin，AGP 8.3.2，minSdk 24 / targetSdk 34）
├── scripts/          运维脚本：deploy_*.py 是部署范例，build_apk.py 打 App 包
│                     lasa-nanshan-platform.nginx.conf / .supervisor.ini 是服务器配置副本
└── README.md
```

改某个功能前先找到对应文件：页面 → `frontend/src/views/Xxx.vue`；接口 → `backend/app/routers/xxx.py`；数据表 → `backend/app/models.py`。

## 3. 本地开发环境

### 3.1 后端（Python 3.11+）

```bash
cd backend
python -m venv venv
# Windows: venv\Scripts\activate     Linux/Mac: source venv/bin/activate
pip install -r requirements.txt
python seed.py                                   # 初始化数据库和默认账号
uvicorn app.main:app --reload --port 18481
```

接口文档（Swagger）：http://127.0.0.1:18481/docs ，调接口先看它。

### 3.2 前端（Node 18+）

```bash
cd frontend
npm install
npm run dev        # 默认 5173 端口，已配置代理到本地后端 18481
```

前端连后端的地址在 `frontend/src/api.js`；开发代理在 `frontend/vite.config.js`。

### 3.3 安卓 App（不改 App 可跳过）

最简单的方式：装 **Android Studio**（Koala 2024.1+，自带 JDK 17 和 Android SDK）→ `File → Open` 打开 `android-app/` → 等 Gradle 同步完 → `Build → Build APK(s)` → 产物在 `app/build/outputs/apk/debug/app-debug.apk`。

命令行方式需要：JDK 17 + Android SDK（platform-34、build-tools 34.0.0）+ Gradle 8.7，参考 `scripts/build_apk.py`（注意里面的构建目录路径是按黄老师机器写的，自己机器要改）。

## 4. 部署到服务器（最重要，严格照做）

`scripts/deploy_incremental.py` 是标准范例（paramiko SSH），核心三步：

```python
# 1. sftp 上传改动的文件到 /opt/lasa-nanshan-platform/ 对应路径
# 2. 重启后端（FastAPI 启动时会 create_all 自动建新表）
supervisorctl restart lasa-nanshan-backend
# 3. 前端有改动时重新构建并重载 nginx
cd /opt/lasa-nanshan-platform/frontend && npx vite build && systemctl reload nginx
```

### ⚠️ 前端构建必看的内存坑（踩过多次）

服务器内存小，直接 `npx vite build` 会 OOM 卡死。正确姿势：

```bash
supervisorctl stop lasa-nanshan-backend          # 先停后端腾内存
NODE_OPTIONS='--max-old-space-size=512' npx vite build
supervisorctl start lasa-nanshan-backend
systemctl reload nginx
```

服务器已调好 `vm.swappiness=60`（/etc/sysctl.conf）并加了 2G 的 `/swapfile2`（已写 fstab），**不要动这两个配置**。

### 部署后必验四件事

1. 浏览器打开 http://106.15.35.204:18480/ 能登录（注意手机输入法自带空格，前后端都做了 trim）
2. 上传一张照片，地图上看到定位点
3. 轨迹列表能加载
4. 外业看板 /ops 能打开

## 5. 发布新版 App

1. `android-app/app/build.gradle` 里 `versionCode` 加 1、`versionName` 递增（如 1.0.0 → 1.1.0）
2. 构建 APK（见 3.3）
3. 用 `scripts/upload_apk.py` 传到服务器 `/static/` 目录，命名如 `lasa-field-app-v1.1.0.apk`
4. 队员手机浏览器访问 `http://106.15.35.204:18480/static/lasa-field-app-v1.1.0.apk` 下载覆盖安装

目前出的是 **debug 签名包**，仅限内部使用；要正式发布再找老师商量签名 keystore。

## 6. 常见坑清单（全是实战踩出来的）

1. **前端构建 OOM 卡死** → 见第 4 节内存坑流程。
2. **Kotlin 字符串模板里别嵌套引号**：`"xxx ${obj["key"]}"` 编译报错，先取局部变量再拼接（TrackService.kt 修过这个）。
3. **手机照片没有定位信息**：微信/QQ 转发会剥掉 EXIF；系统相机要在设置里开「记录地理位置」。App 内拍照不受影响。
4. **改了 models.py 的已有表字段**：`create_all` 只建新表、不改旧表结构，需要手动迁移（ALTER TABLE 或备份后重建）。
5. **推 GitHub 遇到证书/拦截报错**：本机若开着 Scholar 之类加速器，HTTPS 会被拦截。走 SSH 443 通道即可：
   ```bash
   git remote set-url origin ssh://git@ssh.github.com:443/hqq17222/LASA.git
   ```
   先按 GitHub 官方文档配好你自己的 SSH key。
6. **国产 ROM 杀后台**：队员反馈轨迹断记录，先让他们把 App 加入系统「自启动/无限制」白名单。

## 7. 日常维护清单

- **每周**：看一眼后端日志（supervisor 管理的 stdout 日志），确认无持续报错。
- **定期备份**：SSH 上服务器，打包数据库文件 + 上传的照片/视频目录下载到本地（具体路径看 `backend/app/core/config.py`）。
- **新队员账号**：平台「用户管理」页面直接建，野外同学给 analyst，参观的给 viewer。
- **改代码铁律**：本地跑通 → 再部署 → 部署后按第 4 节四项验证。不要直接在服务器上改代码。

## 8. 卡住怎么办

1. 先翻本指南第 6 节和根目录 README.md
2. 看 `scripts/` 里的现成脚本，尤其是 deploy_*.py 的写法
3. 后端报错先看服务器日志；前端问题先 `npm run build` 本地复现
4. 还不行，带着报错截图和复现步骤找黄老师
